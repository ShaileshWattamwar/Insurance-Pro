spring:
  application:
    name: service-registry  # Matches spring.application.name

server:
  port: 8761

eureka:
  instance:
    hostname: localhost

  client:
    fetch-registry: false
    register-with-eureka: false
